#!/usr/bin/python3
name=['a', 'b', 'c', 'd']
'''last_element=name[len(name)-1]'''
print(f"last name in the list is: {name[len(name) - 1]}")
last_element_to_upper=name[-1].upper()
print(f"last element converted to capital case :{last_element_to_upper}")
